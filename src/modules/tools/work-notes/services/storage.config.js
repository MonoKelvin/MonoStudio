/**
 * 工作手记模块存储配置（仅配置，不含存储实现）。
 */
export const workNotesStorageConfig = {
    providerType: 'localStorage',
    storageKey: 'workNotes',
    version: '2.0'
};

export default workNotesStorageConfig;
